services:
  - type: web
    name: ibm-hr-attrition
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run data_analysis.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9